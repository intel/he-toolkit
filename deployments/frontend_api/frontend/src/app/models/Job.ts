

export interface IJob {
    // #region Properties (4)

    org_id: string;
    job_id: string;
    status: string;
    user_id: Number;
    encode: string;
    encrypt: string;
    psi: string;
    decrypt: string;
    decode: string;

    // #endregion Properties (4)
}
